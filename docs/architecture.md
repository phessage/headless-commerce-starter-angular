# Architecture

The starter uses Angular standalone components and signals. The default fixture is synthetic. A production adapter must call `/v1/headless/products` with a publishable key and pass the deployed sandbox gate before production compatibility is claimed.
