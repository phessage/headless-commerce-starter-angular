import{expect,test}from'@playwright/test';test('loads the HTTP catalog and updates signal-driven UI',async({page})=>{const response=page.waitForResponse(r=>r.url().endsWith('/products.json'));await page.goto('/');expect((await(await response).json()).requestId).toBe('synthetic_angular');await expect(page.getByRole('heading',{name:'Field Jacket'})).toBeVisible();await page.getByRole('button',{name:'Add Field Jacket to cart'}).click();await expect(page.getByText('Cart 1')).toBeVisible();await expect(page.getByRole('button',{name:'Add Climbing Rope to cart'})).toBeDisabled()});
